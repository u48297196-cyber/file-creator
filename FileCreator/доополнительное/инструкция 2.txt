Чтобы всё работало как часы, тебе нужно установить эти четыре пакета:

psutil — для мониторинга процессора (CPU) и памяти.

GPUtil — для получения данных о видеокарте (GPU).

tkinter — это стандартная библиотека Python (она уже встроена, устанавливать отдельно её не нужно, она есть у всех).

random и time — тоже входят в стандартный набор Python.
и pip install psutil GPUtil просто введи в консоль и pip install scapy и pip install requests pip install colorama
 и наконец то все готово!! не используйте разведки на сайтах я не нессу ответствность за ето все разведки
тестировал на scanme.nmap.org